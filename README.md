# dotfiles

1. clone this repo into your home directory

2. go into the dotfiles directory

3. run `stow *` to create symlinks for all the dotfiles

# Notes

You can also run `stow <directory>` to only symlink a specific directory.

# Alacritty dependencies

Alacritty requires the [Nerd Fonts] \
Alacritty requires zsh installed

# zsh dependencies

zsh requires bat installed\
zsh requires exa installed\
zsh requires neovim installed